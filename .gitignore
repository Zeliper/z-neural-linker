/target
/dist
# release-local.sh 의 기본 출력 폴더
/dist-local
*.nlproj
*.nlapp
# OMC 세션 상태·계획 기록 (운영 산출물)
/.omc/

# 매니페스트 서명 비밀키. 비밀번호가 없는 키라 파일 자체가 곧 비밀이다 — CI 시크릿으로만 다룬다.
# 공개키(*.pub)는 커밋해도 된다.
packaging/keys/*.key
